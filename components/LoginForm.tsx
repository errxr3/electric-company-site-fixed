'use client';import { signIn } from 'next-auth/react';import { useState } from 'react';import { useRouter } from 'next/navigation';
export function LoginForm(){const[err,setErr]=useState('');const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const res=await signIn('credentials',{email:fd.get('email'),password:fd.get('password'),redirect:false});if(res?.ok)r.push('/admin/leads');else setErr('Неверный логин или пароль')}return <form onSubmit={submit} className="card mx-auto mt-20 grid max-w-md gap-4 p-8"><h1 className="text-3xl font-black">Вход администратора</h1><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Пароль" required/><button className="btn btn-primary">Войти</button>{err&&<p className="text-red-400">{err}</p>}</form>}
